# One-click promotion for this repo (Testing Checklist rel-s19..s21).
#
#   to=rc       canary build → v<base>-rc.1 pre-release + `staging` branch
#   to=release  v<base>-rc.1 → v<base> (releases/latest) + `live` branch
#
# The bytes are the tested build's; only the asset names, the manifest
# (version, channel, urls) and its signature change. Order across repos:
# turbopaneld → turbopanel → ui → website → dev. The three jobs are
# TurboPanel/dev's reusable workflows pinned to ONE sha, passed again as
# dev-ref (that is where the scripts live). Approval = the `release`
# environment (prepare, then finalize). to=release needs the TurboPanel
# Release GitHub App (RELEASE_APP_ID / RELEASE_APP_PRIVATE_KEY) for the
# bare tag and the live push and refuses to start without it; to=rc works
# with GITHUB_TOKEN up to the staging push, which then says how to do it by
# hand until the App exists.

name: Promote

on:
  workflow_dispatch:
    inputs:
      to:
        description: Promote to
        required: true
        type: choice
        options:
          - rc
          - release
      source:
        description: "to=rc: the commit-ish to tag (usually trunk). to=release: the rc tag (vX.Y.Z-rc.1)."
        required: true
        type: string
        default: trunk

permissions:
  contents: write

jobs:
  prepare:
    uses: TurboPanel/dev/.github/workflows/gh-promote.yml@952d34d394347b8ad80e9b0f87efcf0b3233bba8 # dev#16
    with:
      repo-kind: notes-only
      to: ${{ inputs.to }}
      source: ${{ inputs.source }}
      dev-ref: 952d34d394347b8ad80e9b0f87efcf0b3233bba8
    secrets: inherit

  publish:
    needs: prepare
    uses: TurboPanel/dev/.github/workflows/gh-release.yml@952d34d394347b8ad80e9b0f87efcf0b3233bba8 # dev#16
    with:
      version: ${{ needs.prepare.outputs.version }}
      channel: ${{ inputs.to }}
      release-notes: ${{ needs.prepare.outputs.release-notes }}
      assets-artifact-name: ${{ needs.prepare.outputs.assets-artifact-name }}
      manifest-filename: ${{ needs.prepare.outputs.manifest-filename }}
      target-commit: ${{ needs.prepare.outputs.commit }}
    secrets: inherit

  finalize:
    needs: [prepare, publish]
    uses: TurboPanel/dev/.github/workflows/gh-promote-finalize.yml@952d34d394347b8ad80e9b0f87efcf0b3233bba8 # dev#16
    with:
      to: ${{ inputs.to }}
      version: ${{ needs.prepare.outputs.version }}
      commit: ${{ needs.prepare.outputs.commit }}
      branch: ${{ needs.prepare.outputs.branch }}
      manifest-filename: ${{ needs.prepare.outputs.manifest-filename }}
    secrets: inherit

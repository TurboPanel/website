---
title: Memory metrics
description: Memory breakdown, used percent, swap, memory PSI, paging, and memory diagnostics
---

# Memory metrics

Memory charts show **what RAM is being used for**, **whether the host is swapping**, and **whether tasks waited for memory**. Used percent can look comfortable while PSI and major faults say the kernel is reclaiming hard. Swap traffic and disk-backed page reads sit in their own **Paging** group on the console because they mean the same thing — you are out of RAM.

## Overview tile [#overview-tile]

**Memory used** is a derived percent: [used bytes](#memory-breakdown) over capacity. Used excludes reclaimable file cache, so it is what the kernel could **not** hand to a new allocation without evicting something. The tile is the latest value of `derived.memoryUsedPercent`.

## Memory breakdown [#memory-breakdown]

| Series | Wire name | What it is |
| ------ | --------- | ---------- |
| Memory used | `host.memory.usedBytes` | RAM genuinely in use — process heap, kernel, everything the kernel could not reclaim |
| Cached files | `host.memory.cachedFilesBytes` | Page cache — file data the kernel kept because the RAM was idle |

Used is the best single gauge of "can this host take more work". Cached is **not** a problem: Linux uses idle memory as cache on purpose and drops it under pressure. A rising used line is the warning; a large cached number by itself is not. Used plus cached leaving little of the total is a host that has stopped caching — the next step is [pressure](#memory-pressure).

Both series are stored as bytes, not as a percentage of a capacity, so a RAM upgrade does not rewrite last week's history — each bucket is divided by the capacity of the topology generation it was sampled under.

## Memory used [#memory-percent]

`derived.memoryUsedPercent` — used as a percent of capacity, computed on the control plane from used bytes and the known total. Area chart, 0–100%.

Treat the last 10–15% as the danger band on a host that also runs databases or caches — those want the cache. Pair a high used % with [PSI](#memory-pressure) before assuming you must add RAM: a cache-heavy host can sit at 90% used with zero pressure.

## Swap used [#swap-bytes]

`host.memory.swapUsedBytes` — bytes currently on swap. Zero is legitimate when swap is off or unused; the series still plots.

Some swap on a long-lived host is common (idle pages pushed out). A climbing line **together with** swap-in traffic and memory PSI means the host is actively paging, not just holding old pages on disk.

## Swap used % [#swap-percent]

`derived.swapUsedPercent` — swap used as a percent of swap capacity. Hidden-looking zeros still mean “no swap in use,” not “swap is missing.” If the host has no swap at all, used bytes stay at 0 and this percent stays at 0.

## Memory pressure (PSI) [#memory-pressure]

[PSI](/docs/metrics#pressure-stall-information) for RAM. Two series:

| Series | Wire name | What it is |
| ------ | --------- | ---------- |
| Some | `host.memory.pressureSomePercent` | At least one task waited for memory — reclaim, compaction, or swap |
| Full | `host.memory.pressureFullPercent` | **Every** non-idle task waited. The machine stalled |

**Some** is contention. **Full** above zero for more than a spike is thrashing: nothing useful ran because everything was waiting on memory. That is more serious than a high used %.

| What you see | Typical reading |
| ------------ | --------------- |
| Both near 0% | Allocations are being satisfied without stalls |
| Some rising, Full still ~0 | Reclaim is working but costing latency — check [major faults](#memory-major-faults) and [swap I/O](#memory-swap-io) |
| Full leaving zero | Severe. Shed load, add RAM, or find the leak. The host was not doing application work during those samples |

The chart hides when `/proc/pressure/memory` is missing.

## Swap I/O [#memory-swap-io]

| Series | Wire name | What it is |
| ------ | --------- | ---------- |
| Swap in | `host.memory.swapInBytesPerSecond` | Pages read back from swap into RAM |
| Swap out | `host.memory.swapOutBytesPerSecond` | Pages written out to swap |

Swap-out without swap-in is the kernel parking idle pages — often harmless. Swap-**in** is the expensive direction: a process needed those pages and waited on disk. Swap-in plus memory Full PSI is the classic “this host is too small for its working set.”

The chart hides when both series stay empty.

## Major page faults [#memory-major-faults]

`host.memory.majorPageFaultsPerSecond` — faults that required a disk read (not the cheap minor faults satisfied from RAM).

A burst at process start is normal (demand paging). A sustained rate, especially with swap-in, means the working set does not fit. Pair with I/O PSI if the disk is the one paying for it.

## Memory detail [#memory-detail]

These charts are the memory half of the host **diagnostics** row (`host.diagnostics`), reported by every Linux host at every license tier — S1 included — and on self-hosted. They are no longer an organization or server capability toggle; there is nothing to enable. They break RAM into kernel accounting buckets from `/proc/meminfo` and `/proc/vmstat`.

Schema v6 dropped seven of the old detail fields — page tables, kernel stack, the commit limit, and the four active/inactive anonymous/file splits — so the merged row fits alongside the CPU diagnostics. The charts below are what remains.

### Free & cached memory [#memory-detail-primary]

| Series | Wire name | What it is |
| ------ | --------- | ---------- |
| Free | `diagnostics.memoryFreeBytes` | Truly unused pages. Low free with a modest [used](#memory-breakdown) figure is normal |
| Cached | `diagnostics.cachedBytes` | Page cache — file data the kernel kept. Reclaimable under pressure |
| Anonymous | `diagnostics.anonPagesBytes` | Process heap/stack pages. Not file-backed; these become swap |

Anonymous growing without bound is the usual leak signature. Cached growing while used stays flat is the kernel doing its job.

### Slab memory [#memory-detail-slab]

| Series | Wire name | What it is |
| ------ | --------- | ---------- |
| Reclaimable | `diagnostics.slabReclaimableBytes` | Kernel caches it can drop (dentries, inodes) |
| Unreclaimable | `diagnostics.slabUnreclaimableBytes` | Kernel objects it cannot drop without freeing the owner |

A large unreclaimable slab can mean many connections, files, or kernel objects. It does not shrink just because user processes exit.

### Dirty & writeback pages [#memory-detail-dirty]

| Series | Wire name | What it is |
| ------ | --------- | ---------- |
| Dirty | `diagnostics.dirtyBytes` | File pages modified in RAM, not yet written |
| Writeback | `diagnostics.writebackBytes` | Pages the kernel is writing right now |

A large dirty pile followed by writeback and [I/O PSI](/docs/metrics/storage#storage-io-pressure) is a flush storm — backups, logging, or a write-heavy database.

### Shared memory [#memory-detail-other-gauges]

`diagnostics.shmemBytes` — `tmpfs`, POSIX shared memory, and some container overlays. It counts as used but is not owned by any one process, so a host whose used line grows while every process looks small is often filling a `tmpfs` (a `/dev/shm` cache, a build directory on RAM) rather than leaking.

### Committed memory [#memory-detail-commit]

`diagnostics.committedAsBytes` — memory the kernel has promised to processes, overcommit-aware.

Committed can exceed physical RAM on a host that over-allocates (JVMs, databases with huge `mmap`). Watch it as a trend against the machine's total: a committed figure climbing well past RAM plus swap is the shape that ends in an [OOM kill](/docs/metrics/events#oom_kill) even while used % still looks fine.

### Page reclaim [#memory-detail-reclaim]

| Series | Wire name | What it is |
| ------ | --------- | ---------- |
| Direct scan | `diagnostics.pageScanDirectPerSecond` | A process scanned pages itself because it needed memory *now* — this stalls that process |
| kswapd scan | `diagnostics.pageScanKswapdPerSecond` | Background reclaim. Healthier than direct scan |

Direct scan rising with memory Some PSI is the kernel stealing time from applications to free RAM.

### Compaction stalls [#memory-detail-compaction]

`diagnostics.compactionStallsPerSecond` — times the kernel stalled a process to compact fragmented pages (needed for huge pages or large contiguous allocations). Occasional spikes are normal. A sustained rate means fragmentation is costing latency.

## Related

- [Pressure Stall Information](/docs/metrics#pressure-stall-information)
- [CPU pressure](/docs/metrics/cpu#cpu-pressure)
- [I/O pressure](/docs/metrics/storage#storage-io-pressure)

#!/usr/bin/env sh
set -eu
ROOT="$(CDPATH= cd -- "$(dirname "$0")/.." && pwd)"

# Secret scan is never skippable.
"$ROOT/scripts/scan-secrets.sh"

# Future: re-enable lint/typecheck/checks once the toolchain runs inside the Vagrant guest
# (host checkouts share VirtFS with the VM and often lack a usable Node/pnpm tree).
echo "pre-commit: lint/typecheck/checks disabled — secret scan only" >&2
exit 0
